## Summary

- 

## Target Problem

- LeetCode URL:
- Problem slug:
- Related Issue (Required): #

## Verification

- [ ] ローカル実行で動作確認した
- [ ] 実行コマンドを記載した

実行コマンド:

```bash
# 例:
# npm run test
# node --import tsx --test solutions/leetcode/{problem-slug}/solution.test.ts
```

## Implementation Rationale (for review)

- 方針:
- 計算量:
- 代替案と不採用理由:
- 最終決定として残す内容（solution README へ反映）:

## Issue Progress

- [ ] 対応Issueの `status/*` を更新した（`in-review` <-> `review-fix` -> `done` -> `closed`）
- [ ] 必要な検証結果・思考メモを Issue コメントに記載した
- [ ] 今回は進捗更新対象外（理由をSummaryに記載）

## Status Label Update

- [ ] Issue の `status/*` を `status/in-review` に更新した
- [ ] レビュー指摘対応中は `status/review-fix` に更新し、再レビュー依頼時に `status/in-review` に戻した
- [ ] マージ後に `status/done` -> `status/closed` へ更新する

## Checklist

- [ ] ブランチ名がルールに沿っている（`{type}/{problem-name}`）
- [ ] `solve/*` のPRは `Squash and merge` で `main` にマージする
- [ ] すべてのコミットメッセージに `Refs #<issue-number>` を含めた
- [ ] PR本文に `Refs #<issue-number>` を記載した
- [ ] レビューで合意した最終決定を `solutions/leetcode/{problem-slug}/README.md` に反映した
- [ ] 不要なファイル変更が含まれていない
- [ ] `main` への直接 push はしていない
